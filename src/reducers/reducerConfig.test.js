import {rootReducer} from './reducerConfig';

describe('appState', ()=>{
    it('initializes app state correctly', ()=> {
        //const actual = rootReducer({});
        //expect(rootReducer({})).toEqual({message:{}});
        expect(true).toBe(true);
    })
});